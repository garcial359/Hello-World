package com.chf.helloworld;

/**
 * Created by Lucas on 11/8/2014.
 */
public class MyList {//extends LinearLayout implements Checkable{
}
/*
    private static final int[] CHECKED_STATE_SET = {android.R.attr.state_checked};
    private boolean mChecked = false;
    private String[] list;

    public MyList(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    @Override
    public void setChecked(boolean b) {
        if (b != mChecked) {
            mChecked = b;
 //           ToDoActivity.ListAdapter item = (ToDoActivity.ListAdapter) ToDoActivity.getListAdapter().getItem(position);
        }
    }

    @Override
    public boolean isChecked() {
        return mChecked;
    }

    @Override
    public void toggle() {
        setChecked(!mChecked);
    }


}
*/
